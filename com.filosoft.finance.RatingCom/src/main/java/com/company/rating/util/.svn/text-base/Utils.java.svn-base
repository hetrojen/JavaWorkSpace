package com.company.rating.util;

public class Utils {

	public static double round(double d1, int decimalCount){
		double tmp = Math.pow( 10, decimalCount);
		return Math.round(d1 * tmp) / tmp;
	}
}
